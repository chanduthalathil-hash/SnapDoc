<Window x:Class="SnapDoc.Views.WebcamLivePreview"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False" ResizeMode="NoResize"
        WindowStartupLocation="Manual" Cursor="SizeAll">
    <Border BorderBrush="White" BorderThickness="2" CornerRadius="6" Background="Black" ClipToBounds="True">
        <Border.Effect>
            <DropShadowEffect BlurRadius="12" ShadowDepth="1" Opacity="0.6" Color="Black"/>
        </Border.Effect>
        <Image x:Name="PreviewImage" Stretch="UniformToFill"/>
    </Border>
</Window>
